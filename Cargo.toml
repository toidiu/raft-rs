[package]
name = "raft-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
tokio = { version = "1", features = ["time"] }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
futures-test = "0.3"

